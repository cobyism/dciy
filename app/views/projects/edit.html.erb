<div class="page-header">
  <div class="page-title">
    <h1>Configure <span><%= @project.repo %></span></h1>
  </div>
</div>

<%= render 'form' %>

<hr/>

<%= link_to "Remove Project", @project, method: :delete,
  data: { confirm: "Are you sure?" }, class: "button button-danger" %>